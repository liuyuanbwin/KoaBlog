const mac = {
    //启动端口
    port: 3000,

    database:{
        DATABASE:'nodesql',
        USERNAME:'root',
        PASSWORD:'wolaile',
        PORT:'8889',
        HOST:'localhost'
    }
}

const config = mac

module.exports = config